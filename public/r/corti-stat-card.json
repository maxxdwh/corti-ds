{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "corti-stat-card",
  "title": "Corti Stat Card",
  "description": "A compact metric card with optional trend badge.",
  "registryDependencies": [
    "https://ui.shadcn.com/r/styles/default/card.json",
    "https://ui.shadcn.com/r/styles/default/badge.json"
  ],
  "files": [
    {
      "path": "registry/default/corti-stat-card/corti-stat-card.tsx",
      "content": "import { Badge } from \"@/components/ui/badge\"\nimport { Card, CardContent, CardHeader, CardTitle } from \"@/components/ui/card\"\nimport { cn } from \"@/lib/utils\"\n\nexport type CortiStatCardProps = {\n  title: string\n  value: string\n  delta?: string\n  tone?: \"neutral\" | \"positive\" | \"negative\"\n}\n\nexport function CortiStatCard({\n  title,\n  value,\n  delta,\n  tone = \"neutral\",\n}: CortiStatCardProps) {\n  return (\n    <Card className=\"w-full max-w-sm\">\n      <CardHeader className=\"flex flex-row items-center justify-between space-y-0 pb-2\">\n        <CardTitle className=\"text-sm font-medium text-muted-foreground\">\n          {title}\n        </CardTitle>\n        {delta ? (\n          <Badge\n            variant=\"secondary\"\n            className={cn(\n              tone === \"positive\" && \"text-emerald-700\",\n              tone === \"negative\" && \"text-red-700\"\n            )}\n          >\n            {delta}\n          </Badge>\n        ) : null}\n      </CardHeader>\n      <CardContent>\n        <p className=\"text-3xl font-semibold tracking-tight\">{value}</p>\n      </CardContent>\n    </Card>\n  )\n}\n",
      "type": "registry:ui"
    }
  ],
  "type": "registry:ui"
}