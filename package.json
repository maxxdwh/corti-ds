{
  "name": "corti-ds-registry",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "registry:clean": "node scripts/clean-registry.mjs",
    "registry:build": "npm run registry:clean && shadcn build",
    "registry:watch": "npm run registry:clean && shadcn build --watch"
  },
  "devDependencies": {
    "shadcn": "^4.0.5"
  }
}
